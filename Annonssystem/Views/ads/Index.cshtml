@model IEnumerable<Annonssystem.Models.tbl_ads>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ads_category)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ads_content)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ads_price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_annonsorer.an_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ads_category)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ads_content)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ads_price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_annonsorer.an_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ads_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ads_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ads_ID })
        </td>
    </tr>
}

</table>

